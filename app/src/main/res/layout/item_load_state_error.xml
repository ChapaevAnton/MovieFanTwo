<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="loadIsVisible"
            type="Boolean" />

        <variable
            name="errorIsVisible"
            type="Boolean" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_margin="10dp"
            android:id="@+id/error_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="@string/error_load_state_message"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:visibility='@{errorIsVisible ? View.VISIBLE : View.INVISIBLE}' />

        <ProgressBar
            android:id="@+id/progress_load"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:visibility='@{loadIsVisible ? View.VISIBLE : View.INVISIBLE}' />

        <Button
            android:id="@+id/retry_load"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="@string/retry_load"
            android:visibility='@{errorIsVisible ? View.VISIBLE : View.INVISIBLE}' />

    </LinearLayout>
</layout>